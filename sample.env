PORT=3000

# Copy this file to .env and fill in your values (do not commit .env).
# MongoDB Atlas: replace <password> with your actual password (no angle brackets).
# If the password has special characters, URL-encode them (e.g. @ -> %40).
DB_URL=mongodb+srv://theDhruva07:<password>@cluster0.pph0c8k.mongodb.net/url_shortener?retryWrites=true&w=majority&appName=Cluster0